server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Let React Router handle client-side routes after a direct visit or refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
